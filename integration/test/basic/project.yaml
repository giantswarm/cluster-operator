version: 1

test:
  env:
    - "KEEP_RESOURCES=${KEEP_RESOURCES}"
    - "CIRCLE_SHA1=${CIRCLE_SHA1}"
    - "REGISTRY_PULL_SECRET=${REGISTRY_PULL_SECRET}"
    - "CLUSTER_NAME=${CLUSTER_NAME}"
    - "VAULT_TOKEN=mytoken"
    - "COMMON_DOMAIN_GUEST=${COMMON_DOMAIN_GUEST}"
    - "IDRSA_PUB=${IDRSA_PUB}"
    - "GUEST_AWS_ACCESS_KEY_ID=${GUEST_AWS_ACCESS_KEY_ID}"
    - "GUEST_AWS_SECRET_ACCESS_KEY=${GUEST_AWS_SECRET_ACCESS_KEY}"
    - "GUEST_AWS_SESSION_TOKEN=${GUEST_AWS_SESSION_TOKEN}"
    - "HOST_AWS_ACCESS_KEY_ID=${HOST_AWS_ACCESS_KEY_ID}"
    - "HOST_AWS_SECRET_ACCESS_KEY=${HOST_AWS_SECRET_ACCESS_KEY}"
    - "HOST_AWS_SESSION_TOKEN=${HOST_AWS_SESSION_TOKEN}"
    - "AWS_REGION=${AWS_REGION}"
    - "AWS_API_HOSTED_ZONE_GUEST=${AWS_API_HOSTED_ZONE_GUEST}"
    - "AWS_INGRESS_HOSTED_ZONE_GUEST=${AWS_INGRESS_HOSTED_ZONE_GUEST}"
    - "TESTED_VERSION=${TESTED_VERSION}"
    - "GITHUB_BOT_TOKEN=${GITHUB_BOT_TOKEN}"
