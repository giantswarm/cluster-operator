apiVersion: apps/v1
kind: Deployment
metadata:
  name: cluster-operator
  namespace: {{ .Values.namespace }}
  labels:
    app: cluster-operator
    version: {{ .Chart.Version }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cluster-operator
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cluster-operator
        version: {{ .Chart.Version }}
      annotations:
        releasetime: {{ $.Release.Time }}
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: cluster-operator
                  version: {{ .Chart.Version }}
              topologyKey: kubernetes.io/hostname
            weight: 100
      volumes:
      - name: cluster-operator-configmap
        configMap:
          name: cluster-operator-configmap
          items:
          - key: config.yml
            path: config.yml
      serviceAccountName: cluster-operator
      securityContext:
        runAsUser: {{ .Values.userID }}
        runAsGroup: {{ .Values.groupID }}
      containers:
      - name: cluster-operator
        image: {{ .Values.Installation.V1.Registry.Domain }}/giantswarm/cluster-operator:[[ .SHA ]]
        args:
        - daemon
        - --config.dirs=/var/run/cluster-operator/configmap/
        - --config.files=config
        volumeMounts:
        - name: cluster-operator-configmap
          mountPath: /var/run/cluster-operator/configmap/
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 30
          timeoutSeconds: 1
        resources:
          requests:
            cpu: 100m
            memory: 220Mi
          limits:
            cpu: 100m
            memory: 220Mi
      imagePullSecrets:
      - name: cluster-operator-pull-secret
