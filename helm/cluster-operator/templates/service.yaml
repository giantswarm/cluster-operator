apiVersion: v1
kind: Service
metadata:
  name: cluster-operator
  namespace: {{ .Values.namespace }}
  labels:
    app: cluster-operator
  annotations:
    prometheus.io/scrape: "true"
spec:
  ports:
  - port: 8000
  selector:
    app: cluster-operator
